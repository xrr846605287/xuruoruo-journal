let posts=JSON.parse(localStorage.getItem('posts')||'[]');function render(){const feed=document.getElementById('feed');feed.innerHTML='';posts.forEach((post,index)=>{const div=document.createElement('div');div.className='card';div.innerHTML=`<img src="${post.image}"><div class="content"><h4>${post.title}</h4><p>${post.desc}</p>❤️ ${post.likes}<button onclick="like(${index})">点赞</button></div>`;feed.appendChild(div);});}function addPost(){const file=document.getElementById('imageInput').files[0];const title=document.getElementById('titleInput').value;const desc=document.getElementById('descInput').value;const reader=new FileReader();reader.onload=function(e){posts.unshift({image:e.target.result,title,desc,likes:0});localStorage.setItem('posts',JSON.stringify(posts));render();};if(file)reader.readAsDataURL(file);}function like(index){posts[index].likes++;localStorage.setItem('posts',JSON.stringify(posts));render();}render();